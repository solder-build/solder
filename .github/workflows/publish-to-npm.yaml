name: Publish to NPM

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      version_bump:
        description: "Version bump type (major, minor, patch)"
        required: false
        default: "patch"
        type: choice
        options:
          - major
          - minor
          - patch

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.0.0
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: |
          cd packages/core
          pnpm run build
          cd ../cli
          pnpm run build

      - name: Determine version bump type
        id: version_type
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            VERSION_BUMP="${{ github.event.inputs.version_bump }}"
          else
            # Get the last commit message
            COMMIT_MSG=$(git log -1 --pretty=%B)
            
            if [[ "$COMMIT_MSG" == *"[major]"* ]]; then
              VERSION_BUMP="major"
            elif [[ "$COMMIT_MSG" == *"[minor]"* ]]; then
              VERSION_BUMP="minor"
            else
              VERSION_BUMP="patch"
            fi
          fi

          echo "bump_type=$VERSION_BUMP" >> $GITHUB_OUTPUT
          echo "Version bump type: $VERSION_BUMP"

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Bump version for @solder-build/core
        id: bump_core
        run: |
          cd packages/core
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          echo "Current version: $CURRENT_VERSION"

          npm version ${{ steps.version_type.outputs.bump_type }} --no-git-tag-version

          NEW_VERSION=$(node -p "require('./package.json').version")
          echo "New version: $NEW_VERSION"
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: Bump version for create-solder
        id: bump_cli
        run: |
          cd packages/cli
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          echo "Current version: $CURRENT_VERSION"

          npm version ${{ steps.version_type.outputs.bump_type }} --no-git-tag-version

          NEW_VERSION=$(node -p "require('./package.json').version")
          echo "New version: $NEW_VERSION"
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: Commit version bumps
        run: |
          git add packages/core/package.json packages/cli/package.json
          git commit -m "chore: bump versions to ${{ steps.bump_core.outputs.new_version }} [${{ steps.version_type.outputs.bump_type }}]" || echo "No changes to commit"
          git push

      - name: Publish @solder-build/core to NPM
        run: |
          cd packages/core
          npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish create-solder to NPM
        run: |
          cd packages/cli
          npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create Git tags
        run: |
          git tag "core-v${{ steps.bump_core.outputs.new_version }}"
          git tag "cli-v${{ steps.bump_cli.outputs.new_version }}"
          git push --tags

      - name: Summary
        run: |
          echo "## 📦 Published Packages" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ **@solder-build/core** - v${{ steps.bump_core.outputs.new_version }}" >> $GITHUB_STEP_SUMMARY
          echo "✅ **create-solder** - v${{ steps.bump_cli.outputs.new_version }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Version bump type: **${{ steps.version_type.outputs.bump_type }}**" >> $GITHUB_STEP_SUMMARY
